/// 当前版本号（唯一来源是 `pubspec.yaml` 的 `version:`，本文件必须与它同步）。
///
/// 「两处同步」原本只是 `AGENTS.md` 里的一句约定，靠人记 —— 结果 v0.6.6 到
/// v0.6.9 四个版本都只改了 pubspec，这里一直停在 `0.6.5`。代价不只是「我的」
/// 页显示的版本号不对：`lastSeenVersion` 也是拿它写的，它不变就等于
/// **升级后的「版本更新」弹窗再也不弹**；「检查更新」里「当前版本」也一直是
/// 错的，会给已经装上的版本再挂一个「下载」按钮。
///
/// 现在由 `test/app_info_test.dart` 盯着两处一致，改漏了会直接把测试打红。
const String appVersion = '0.6.13';
