<?xml version="1.0" encoding="utf-8"?>
<!--
  4×5 整月。固定尺寸、不可拉伸（本轮重做的核心）。配方与 `widget_week_strip_info`
  逐条同源，只把高度与预览换成这一张的，理由都在那一份里；这里只记不同点。

  三张卡里最高：minHeight 320dp 要给「月份标题 + 周几行 + 6 行日期格」全部排下，
  且每格还要盛得下一个日数字。`targetCellHeight="5"` 给 Android 12+ 的桌面按格子摆。
-->
<appwidget-provider xmlns:android="http://schemas.android.com/apk/res/android"
    android:minWidth="250dp"
    android:minHeight="320dp"
    android:targetCellWidth="4"
    android:targetCellHeight="5"
    android:resizeMode="none"
    android:widgetCategory="home_screen"
    android:updatePeriodMillis="0"
    android:initialLayout="@layout/widget_placeholder"
    android:previewLayout="@layout/widget_preview_month"
    android:previewImage="@drawable/widget_preview_month_img" />
