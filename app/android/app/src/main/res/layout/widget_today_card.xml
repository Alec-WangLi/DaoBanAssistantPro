<?xml version="1.0" encoding="utf-8"?>
<!--
  今日卡片。照搬 App 底栏信息卡的**完整版**（calendar_screen.dart 的 _infoCard），
  元素与配方逐项见 spec §7。

  ⚠️ 三个徽章（今天 / N 项待办 / 已调班）都是「FrameLayout 固定宽高 + 底下铺一张淡染
  胶囊位图 + 上面一个居中 TextView」。**宽度必须写死** —— RemoteViews 在渲染时量不到
  文字宽度，只能按固定宽度画、文字居中、超长省略。宽度按**英文**留余量
  （「Shift changed」比「已调班」长得多）。

  ⚠️ 根上**不画卡片底**（没有 `android:background`）：本布局永远被塞进
  `widget_today_standalone` 的槽位里，外壳根已经铺了一层同一张卡片底
  （`renderTodayCard` 也不再设）。两层同图叠放会多出一圈圆角描边、悬在外壳边框内侧。

  ⚠️ 这是 **4×3 独立卡**的排版（根 padding 22dp、日期 22sp、班次 18sp、行距 16dp、
  农历两行）—— 不再跟着网格装配壳走。字号与间距在 2026-09-20 的真机反馈后整体提了一档：
  原先 18dp padding / 20sp / 12dp 时上下会各留一截空白（用户原话「上下留了好大的空白」），
  提一档把内容从约 202dp 撑到 **实测约 232dp**。**行高与结构没动**（仍只有一个日期行 +
  农历两行 + 班次行 + 时间行 + 班组行），改的只有字号、间距与徽章/色条这几处尺子上的一档。

  ⚠️ **没到裁决时定的「余量 ≤ 25dp」**：4×3 外壳实测 266dp，232dp 内容留出约 34dp 余量
  （改前约 65dp，确实收了一半）。短的那 9dp 出在这一档自己身上 —— 表里各处的 +Δ 加起来
  约 +29dp，而「245dp」这个目标数隐含的是 +43dp。要么再提一档字号，要么承认这版就到这里。
-->

<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/wg_tc_root"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:padding="22dp">

    <!-- 1. 日期行：色条 + 日期 + 今日徽章 + 待办徽章 -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center_vertical">

        <ImageView
            android:id="@+id/wg_tc_bar"
            android:layout_width="4dp"
            android:layout_height="24dp"
            android:scaleType="fitXY"
            android:contentDescription="@null" />

        <TextView
            android:id="@+id/wg_tc_date"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginStart="10dp"
            android:textSize="22sp"
            android:textStyle="bold"
            android:maxLines="1"
            android:ellipsize="end" />

        <FrameLayout
            android:id="@+id/wg_tc_today_wrap"
            android:layout_width="52dp"
            android:layout_height="24dp"
            android:layout_marginStart="8dp">

            <ImageView
                android:id="@+id/wg_tc_today_bg"
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:scaleType="fitXY"
                android:contentDescription="@null" />

            <TextView
                android:id="@+id/wg_tc_today_text"
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:gravity="center"
                android:textSize="11sp"
                android:maxLines="1"
                android:ellipsize="end" />
        </FrameLayout>

        <FrameLayout
            android:id="@+id/wg_tc_todo_wrap"
            android:layout_width="84dp"
            android:layout_height="24dp"
            android:layout_marginStart="6dp">

            <ImageView
                android:id="@+id/wg_tc_todo_bg"
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:scaleType="fitXY"
                android:contentDescription="@null" />

            <TextView
                android:id="@+id/wg_tc_todo_text"
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:gravity="center"
                android:textSize="11sp"
                android:maxLines="1"
                android:ellipsize="end" />
        </FrameLayout>
    </LinearLayout>

    <!-- 2. 农历。4×3 放的是**完整描述**（快照的 `lunarFull`，App 完整版信息卡同源），
         所以两行、14sp；法定节假日走 holiday 红由渲染侧给色。 -->
    <TextView
        android:id="@+id/wg_tc_lunar"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="16dp"
        android:textSize="14sp"
        android:maxLines="2"
        android:ellipsize="end" />

    <!-- 3. 班次行：色点 + 班次名 + 已调班徽章 -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="16dp"
        android:orientation="horizontal"
        android:gravity="center_vertical">

        <ImageView
            android:id="@+id/wg_tc_dot"
            android:layout_width="12dp"
            android:layout_height="12dp"
            android:scaleType="fitXY"
            android:contentDescription="@null" />

        <TextView
            android:id="@+id/wg_tc_shift"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginStart="6dp"
            android:textSize="18sp"
            android:textStyle="bold"
            android:maxLines="1"
            android:ellipsize="end" />

        <FrameLayout
            android:id="@+id/wg_tc_adj_wrap"
            android:layout_width="96dp"
            android:layout_height="24dp"
            android:layout_marginStart="6dp">

            <ImageView
                android:id="@+id/wg_tc_adj_bg"
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:scaleType="fitXY"
                android:contentDescription="@null" />

            <TextView
                android:id="@+id/wg_tc_adj_text"
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:gravity="center"
                android:textSize="11sp"
                android:maxLines="1"
                android:ellipsize="end" />
        </FrameLayout>
    </LinearLayout>

    <!-- 4. 时间。另起一行：挤在班次名那一行会在窄宽下把时间全截掉（App 里踩过）。
         左边缩进 18dp = 色点 12 + 间距 6，与上一行的班次名对齐。 -->
    <TextView
        android:id="@+id/wg_tc_time"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="8dp"
        android:layout_marginStart="18dp"
        android:textSize="13sp"
        android:maxLines="1"
        android:ellipsize="end" />

    <!-- 5. 其他班组：固定 4 个槽位（多于 4 个的丢掉，spec §7 已记这个上限）。
         槽位是 FrameLayout + 等分权重。**chip 填满槽位靠的是嵌套根自己显式声明的
         match_parent 被槽位尊重** —— 不是槽位用默认 LayoutParams 补齐（spec §5.4
         真机实测更正过这条口径）。所以 widget_crew_chip.xml 根上那行显式 match_parent
         **不能删**：删掉它会缩成一小团且不报错。 -->
    <LinearLayout
        android:id="@+id/wg_tc_crew_row"
        android:layout_width="match_parent"
        android:layout_height="22dp"
        android:layout_marginTop="16dp"
        android:orientation="horizontal">

        <FrameLayout
            android:id="@+id/wg_tc_crew_slot1"
            android:layout_width="0dp"
            android:layout_height="match_parent"
            android:layout_weight="1" />

        <FrameLayout
            android:id="@+id/wg_tc_crew_slot2"
            android:layout_width="0dp"
            android:layout_height="match_parent"
            android:layout_weight="1" />

        <FrameLayout
            android:id="@+id/wg_tc_crew_slot3"
            android:layout_width="0dp"
            android:layout_height="match_parent"
            android:layout_weight="1" />

        <FrameLayout
            android:id="@+id/wg_tc_crew_slot4"
            android:layout_width="0dp"
            android:layout_height="match_parent"
            android:layout_weight="1" />
    </LinearLayout>
</LinearLayout>
