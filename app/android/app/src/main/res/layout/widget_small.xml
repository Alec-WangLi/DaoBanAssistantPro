<?xml version="1.0" encoding="utf-8"?>
<!--
  小档：今天一张牌。尺寸约 2×2（180×180dp）。
  结构从上到下：日期行 → 班次区（色条 + 班次名 + 时间）→ 分隔线 → 明天预告行。
  色条是 ImageView 而不是 View —— 色块按「用户自选的任意 ARGB」运行期画成圆角
  位图（见 WidgetChip），而 setImageViewBitmap 只认 ImageView。
-->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/wg_s_root"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="@drawable/widget_card_light"
    android:padding="14dp">

    <!-- 日期行：左「今天 · 周四」，右「9月18日」 -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal">

        <TextView
            android:id="@+id/wg_s_relative"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:textSize="12sp"
            android:textStyle="bold"
            android:maxLines="1" />

        <TextView
            android:id="@+id/wg_s_weekday"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:layout_marginStart="6dp"
            android:textSize="12sp"
            android:maxLines="1" />

        <TextView
            android:id="@+id/wg_s_date"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:textSize="12sp"
            android:maxLines="1" />
    </LinearLayout>

    <!-- 班次区：色条 + 班次名 + 时间。占满纵向剩余空间，底对齐看着更稳。 -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:layout_marginTop="8dp"
        android:orientation="horizontal"
        android:gravity="center_vertical">

        <ImageView
            android:id="@+id/wg_s_bar"
            android:layout_width="6dp"
            android:layout_height="match_parent"
            android:scaleType="fitXY"
            android:contentDescription="@null" />

        <LinearLayout
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:layout_marginStart="10dp"
            android:orientation="vertical">

            <TextView
                android:id="@+id/wg_s_shift"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:textSize="24sp"
                android:textStyle="bold"
                android:maxLines="1"
                android:ellipsize="end" />

            <TextView
                android:id="@+id/wg_s_time"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_marginTop="2dp"
                android:textSize="14sp"
                android:maxLines="1"
                android:ellipsize="end" />
        </LinearLayout>
    </LinearLayout>

    <!-- 分隔线用 ImageView 而不是 <View>：白名单是靠 `@RemoteView` 注解过滤的，
         而 `android.view.View` **没有**这个注解（`Space` 也没有）。用 <View> 的
         后果是宿主在 apply() 阶段抛 InflateException，整张卡片渲染不出来，
         而不是只丢这一条线。 -->
    <ImageView
        android:id="@+id/wg_s_divider"
        android:layout_width="match_parent"
        android:layout_height="1dp"
        android:layout_marginTop="8dp"
        android:contentDescription="@null" />

    <TextView
        android:id="@+id/wg_s_next"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="8dp"
        android:textSize="12sp"
        android:maxLines="1"
        android:ellipsize="end" />
</LinearLayout>
