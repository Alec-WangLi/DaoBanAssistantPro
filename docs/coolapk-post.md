# 酷安发帖稿 · 倒班助手Pro v0.9.0

## 评论区回复用的短文案（每发一版给一段）

> 用户是在**自己那篇介绍帖下面的评论区**里回复，不是发新帖 —— 所以**只讲这一版
> 跟上个正式版的主要区别**，不超过 10 行；内部重构、测试、措辞改动一律不写。
> 配套图：`docs/images/v090-*.png`（六张，顺位见文件末尾那一节）。

### v0.9.0（对比上一个正式版 v0.8.0）

v0.9.0 正式版已发，跟上个正式版比主要多了这些：

1. 桌面小组件：三张固定卡 —— 本周条 / 今日卡 / 整月
2. 日历上按天改班：请假、跟同事换班点一下就行，不动整套排班
3. 我的模板：调好的班表存下来，下次新建排班直接用
4. 零点班闹钟改排上班前一天（以前排在班后，等于白设）
5. 班组撞班现在会点名，并一键按周期长度均分

图见下面几张。老用户「我的 → 检查更新」里可以直接升级：
https://github.com/Alec-WangLi/DaoBanAssistantPro/releases/tag/v0.9.0

---

> 下面这份是**发帖用的正文草稿**：正文按酷安编辑器能接受的纯文本写，图片按顺序插在
> 标注的位置。配套图在 `work/promo-out/`（大图，不入库），由
> `python scripts/make_promo_images.py` 生成。
>
> 图序（与正文里的【图 N】一一对应）：
> 1. `cover.png` —— 封面
> 2. `shot-calendar.png` —— 日历
> 3. `shot-alarm.png` —— 班次闹钟
> 4. `shot-ringing.png` —— 全屏响铃
> 5. `shot-editor.png` —— 排班编辑器
> 6. `shot-templates.png` —— 倒班方式模板
> 7. `shot-todos.png` —— 待办
> 8. `shot-permissions.png` —— 权限自检
> 9. `shot-calendar-dark.png` —— 深色模式
> 10. `shot-wide.png` —— 横屏分栏

---

## 标题

自己写了个倒班助手：排班日历 + 班次闹钟，无广告无账号，纯本地

## 正文

【图 1 cover.png】

倒班的人大概都经历过这两件事：半夜爬起来看明天几点上班；以及设了闹钟，结果班次一换就全乱了。

市面上的倒班表 App 不少，但大多要么广告多、要么要账号、要么闹钟就是个普通的固定闹钟，跟你这周到底上什么班没关系。所以我干脆自己写了一个——**把「排班」和「闹钟」真正连起来**。

完全自研，Flutter 写的，开源（MIT）。无账号、无广告、无后端，所有数据只存在你自己手机里。

**核心就一句话：排班表排好，闹钟自动跟着排班走。**

比如四班两倒，你只需要建一次排班——白班 8:30 上班，上夜班 20:30 上班——之后未来 60 天的闹钟它自己排好、自己续排，不用管。以后换班组、调班次时间，改一次，闹钟跟着重新排。

【图 2 shot-calendar.png】

**排班日历**

（这一版新增）**某几天单独改班**：点底栏那行班次、或者长按格子拖选一段（可跨周、不跨月），就能给这几天单独指定班次 —— 请假挑个休班、跟同事换班挑对方那个班，不动整套排班。被改过的那天格子上有个小圆点、信息卡写着「已调班」，随时能一键「恢复轮转」。

日历每格是一个**带底色的班次胶囊**，一眼扫出这个月怎么倒；选中那天的胶囊变实心。同时还带农历、节气、法定节假日和调休标记（调休上班日会打一个「班」字）。

下面那张卡片是当天的详情：几点到几点、联动闹钟是几点，以及同一天其他班组的班次——交接班的时候对一眼就知道对面是谁在岗。

点某天就看某天的，也可以直接跳到任意年月。

【图 3 shot-alarm.png】

**班次闹钟**

这是我认为这个 App 最值钱的部分。

- 每个班次可以单独设一个**响铃钟点**（比如白班 07:00、上夜班 19:30），跟排班自动联动。零点班（00:00 上班）的闹钟会排在上班前 1 小时、也就是前一天晚上，列表里写明「前一天」
- 闹钟页列出未来 30 天将要响的班次闹钟，**每一条都能单独开关**（比如某天你请假，关掉那一天就行，不影响别的）
- 响过的那条自动从列表里消失，不会剩一堆历史噪音
- 另外还有独立于排班的自定义闹钟：一次性 / 每天 / 每周

技术上用的是系统精确闹钟（setAlarmClock）+ 前台服务 + 全屏响铃界面，锁屏也能响，不太会被系统省电策略掐掉。App 里有个「测试闹钟」，一键排一个 10 秒后的，想验证整条链路通不通随时可以试。

【图 4 shot-ringing.png】

响铃界面长这样，上滑关闭、点「再睡一会」贪睡 5 分钟，跟手不跟手一滑就知道。

【图 5 shot-editor.png】

**排班可以随便改**

内置了 20 种常见倒班方式，新建排班时选一个最像的当起点：白夜休休、白白夜夜休休、四班三倒、五班三倒（5 天一轮 / 10 天一轮两套）、六班三倒、五班四倒、六班四倒、上 24 休 24/48/72、长白班、做一休一 / 做二休二 / 做四休二、做六休一、大小周、两班倒、DuPont……支持关键词搜索。**自己调好的那套还能点「存为模板」存下来**（编辑器右上角），下次新建直接从「我的模板」里选。

【图 6 shot-templates.png】

选完就是一个普通方案，随便改：班次时间、颜色、联动闹钟改一次，周期表里所有同名班次同步生效。周期长度 1–60 天自己定，班组用「周期起始日」表达——哪天我这组从周期第一天开始，各班组之间差几天一填就错开了。

编辑器上面还有一条未来 14 天的实时预览，改什么都立刻看得到对不对。

【图 6.5 桌面小组件（真机截图：三张卡在桌面上的样子）】

**桌面小组件**

三张固定尺寸的卡，放在桌面上不用打开 App 就能看：**本周条**（4×1，今天所在这一周）、**今日卡**（4×3，App 底栏那张信息卡的完整版：日期、今天与待办徽章、农历、班次与时间、已调班、其他班组）、**整月**（4×5，42 格月历）。放置后不能拉伸 —— 想要别的尺寸就换一张加。在 App 里改了排班，桌面立刻跟着变；点某一天直接跳到那天的日历。

小米 / HyperOS 上要进「支持小部件的应用 → 安卓小部件」才找得到（长按桌面空白处 → 添加小部件）。

【图 7 shot-todos.png】

**待办**

交班、开会这类事可以记下来，带时间与提前提醒；办完了勾一下，变暗加删除线。

【图 7.5 我的模板（真机截图：选择页最上面的「我的模板」一组）】

**我的模板**

调好一套排班之后，编辑器右上角点一下就能存成模板；下次「新建排班」时它排在选择页最上面，选中就建出一套一模一样的结构。自己厂里的班表不用每次从头搭。存下来的可以在那一组旁边的「管理」里改名、删除。

【图 8 shot-permissions.png】

**关于权限，说点实在的**

国产 ROM 上闹钟不响，十次有九次不是 App 的锅，是权限没给全。所以「我的」页里放了一张权限自检卡，按用途分三组（基础提醒 / 弹出响铃界面 / 后台与开机），**每一行直接写清「不开会怎样」**，没开的一键跳到对应系统设置页。

「自启动」和小米那两项系统不给 App 检测，所以那几行永远显示「去查看」，点进去自己确认——不假装能检测。小米机型多两行：「后台弹出界面（小米）」和「锁屏显示（小米）」，这是小米单独设的两项权限（系统里分别叫「Open new windows while running in the background」和「Show on Lock screen」），不开的话闹钟到点只会响、不会弹出响铃界面；实测这两项都开上之后，App 退到后台、甚至锁屏息屏，闹钟到点都能自动点亮屏幕弹出响铃界面。另外这一版起**重启手机后闹钟与待办提醒都不再丢**（开机自动排回去），前提是「自启动」开着。

【图 9 shot-calendar-dark.png】

外观方面：深空蓝紫的液态玻璃风格，跟随系统 / 浅色 / 深色三档，5 种主色调可选，中英文双语。低端机会自动关掉背景模糊，不至于为了好看把手机拖卡。

【图 10 shot-wide.png】

横屏、平板车机这类宽屏上，日历会自动变成左右分栏——左边日期网格、右边当天信息，日期格不会被压扁。小米小窗和分屏也是按实测尺寸专门适配过的。

**下载**

GitHub Release（含更新日志与 SHA256）：
https://github.com/Alec-WangLi/DaoBanAssistantPro/releases/latest

开源地址（MIT，欢迎提 issue）：
https://github.com/Alec-WangLi/DaoBanAssistantPro

App 内自带「检查更新」，装上之后以后有新版本直接在 App 里下载安装，不用再回来翻帖子。

**说两句不足**

- 法定节假日数据目前只做到 2026 年底（内置的是 2025/2026 两年的国务院放假安排）。2027 年的安排一般每年 11 月才发布，发布后我会跟着更新一版。在那之前，2027 年及以后的日期不会标节假日——这点先说明白，免得明年一月有人以为是 bug。
- 小米 / 华为的「免解锁弹全屏」**不是系统限制**——早先版本我以为是，接了真机调试才查清：其实是小米单独设的一项系统权限（「后台弹出界面」+「锁屏显示」）没开，MIUI 会静默拒绝 App 从后台弹出界面。现在「我的 → 权限」里能一键跳到那一页去开，开完退后台、锁屏、息屏都能自动弹出响铃界面。
- **关机的时候闹钟不会响**——关机状态下没有任何应用在运行，这一点绕不过去。重启之后闹钟与待办提醒会自动恢复（开机自动排回去），但要等手机**第一次解锁之后**：这是安卓的限制，设了锁屏密码的设备，「开机完成」这个广播要等解锁才发给应用。
- 只有 Android（minSdk 26，即 Android 8.0 及以上）。iOS 没有，因为我没有 Mac。

以上。如果这个正好解决你的问题，欢迎试用；有问题或者想要什么功能，回帖或者去 GitHub 提 issue 都行。

---

## 版本更新图的顺位（评论区回复用，`docs/images/`）

与上面那篇「介绍帖」的图分开：这一组是**每个版本更新时**在评论区补的图，
每张只讲这一版跟上个正式版的区别（生成脚本 `scripts/make_update_images.py`）。

| 顺位 | 文件 | 讲什么 |
|---|---|---|
| 1 | `v090-cover.png` | 首图：这一版多了哪几件事 |
| 2 | `v090-widgets.png` | 桌面小组件（真机双屏：本周条+今日卡 / 整月） |
| 3 | `v090-override.png` | 日历上按天改班 |
| 4 | `v090-templates.png` | 我的模板 |
| 5 | `v090-midnight.png` | 零点班闹钟改排上班前一天 |
| 6 | `v090-clash.png` | 班组撞班点名 + 一键均分 |

历史：`v080-*.png` 是上一个正式版那一组。
